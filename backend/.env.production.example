# Production Environment Variables for Backend
# Copy this file to .env.production and fill in the actual values
# DO NOT commit .env.production to version control!

# Server Configuration
NODE_ENV=production
PORT=5000

# Database Connection
# Use the Internal Database URL from Render PostgreSQL dashboard
DATABASE_URL=postgresql://user:password@host:5432/therapy_tracker

# JWT Secret
# Generate using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_very_long_and_secure_random_string_here_64_plus_characters

# Email Service Configuration
# For Gmail: Enable 2FA and use App Password
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your_gmail_app_password_here
EMAIL_FROM=Therapy Tracker <noreply@therapytracker.com>

# Frontend URL
# Update this with your actual frontend URL after deployment
FRONTEND_URL=https://therapy-tracker.onrender.com

# Cloudinary Configuration (for profile picture storage)
# Sign up at https://cloudinary.com and get these from your dashboard
# REQUIRED for production - Render has ephemeral storage
CLOUDINARY_CLOUD_NAME=your_cloud_name_here
CLOUDINARY_API_KEY=your_api_key_here
CLOUDINARY_API_SECRET=your_api_secret_here
